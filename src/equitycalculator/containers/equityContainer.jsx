import { connect } from 'react-redux'
import { Equity } from '../'

const mapStateToProps = (state) => {
    return {}
}

export const EquityContainer = connect(
    mapStateToProps
)(Equity)
