import React, { Component } from 'react';
import { Grid } from 'react-bootstrap'

class App extends Component {
    render() {
      return (
        <Grid> 
          <header className="App-header">
            <h1 className="App-title">Free Tilt</h1>
          </header>
        </Grid >
      );
    }
}

export default App;
